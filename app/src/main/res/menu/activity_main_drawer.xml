<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="@string/categories" android:id="@+id/categoriesSubMenu">
        <menu>
            <group android:checkableBehavior="single" android:id="@+id/categoriesGroup">
                <item
                    android:id="@+id/nav_addNew"
                    android:icon="@drawable/ic_add_black_24dp"
                    android:title="@string/new_category" />
                <item
                    android:id="@+id/nav_all"
                    android:icon="@drawable/ic_label_black_24dp"
                    android:title="@string/all" />
            </group>
        </menu>
    </item>

    <item android:title="@string/rewards">
        <menu>
            <item
                android:id="@+id/nav_rewards"
                android:icon="@drawable/ic_menu_gallery"
                android:title="@string/rewards" />
        </menu>
    </item>

</menu>
